<%= semantic_form_for(@paymenttype, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @paymenttype.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@paymenttype.errors.count, "error") %> prohibited this paymenttype from being saved:</h4>

      <ul>
      <% @paymenttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
    <%= f.actions do %>    
      <%= f.action :submit, :as => :button , :button_html => {:class => "btn btn-primary" , :disable_with => 'Wait...' }%>
    <% end %> 
    </div>
  </div>
<% end %>
