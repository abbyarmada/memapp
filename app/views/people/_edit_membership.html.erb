<%= simple_form_for(@person.member) do |form|  %>
  <%= render :partial => 'membership' , :locals => { :form => form } %>
 <div class="form-actions">
    <%= form.button :submit , :class => 'btn-primary' %>
  </div>

<% end %>