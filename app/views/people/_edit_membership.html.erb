<%= semantic_form_for(@person.member, :namespace => @person.id) do |form|  %>
  <%= render :partial => 'membership' , :locals => { :form => form ,:form2 => false, :buttons => true ,:button_label => 'update Membership'  }  %>
<% end %>